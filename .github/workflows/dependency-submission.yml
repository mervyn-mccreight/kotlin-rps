name: Dependency Submission

on:
  push:
    branches:
      - main

jobs:
  dependency-submission:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.3
      - uses: gradle/actions/wrapper-validation@v3.3.1
      - uses: gradle/actions/dependency-submission@v3.3.1
