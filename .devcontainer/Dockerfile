# Use the official Codespaces base image with Ubuntu and common tools
FROM mcr.microsoft.com/devcontainers/base:ubuntu

# Optional: Add custom tools or dependencies here
# RUN apt-get update && apt-get install -y ...

# Use devcontainer features for Java + Gradle (defined in devcontainer.json)
